/* Defino los conceptos:

prototype: El prototipo (prototype) son mecanismos medianteslos cuales los objetos heredan metodos entre sí.

constructors (de clases):Son funciones constructoras que crean objetos los cuales van a pertenecer a una misma clase.
